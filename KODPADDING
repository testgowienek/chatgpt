html{
  box-sizing: border-box;
}
*{
  box-sizing: inherit;
}

body {
  font-family: Georgia, serif;
  font-size: 100%;
  background-color: white;
}

/* linki */
a:link, a:visited {
  color: #DC6903;
}
a:hover {
  color: #F9AB33;
}

/* HEADER */
header {
  color: white;
  background: url(images/croissants_banner.jpg) no-repeat center center;
  background-size: cover;
  text-align: center;
  height: 15em; /* zadanie */
}

header p {
  font-style: italic;
  font-size: 1.2em;
}

/* NAV + FOOTER */
nav, footer {
  font-family: verdana, sans-serif;
  background-color: #783F27;
}

/* MAIN */
main {
  font-family: 'Stint Ultra Expanded', cursive;
  line-height: 1.8em;
  color: #555;
}

/* zadanie */
main * {
  padding: 1em;
}

/* ASIDE */
aside {
  background: url(images/scallop.png) repeat-y left top;
  background-color: #F6F3ED;
  padding: 1em;        /* zadanie */
  padding-left: 45px;  /* zadanie */
}

/* FOOTER */
footer {
  color: #EADDC4;
  text-align: center;
  font-size: .8em;
  padding: 1em; /* zadanie */
}

/* reszta */
h3 {
  color: #7A0002;
}
.day {
  color: #783F27;
}








-----------


html{
  box-sizing: border-box;
}
*{
  box-sizing: inherit;
}

body {
  font-family: Georgia, serif;
  font-size: 100%;
  background-color: white;
}

/* Style łączy */
a:link,
a:visited {
  color: #DC6903;
}
a:focus,
a:hover,
a:active {
  color: #F9AB33;
}

/* =========================
   NAGŁÓWEK
   ========================= */
header {
  color: white;
  background: url(images/croissants_banner.jpg) no-repeat center center;
  background-size: cover;
  text-align: center;
  height: 15em;          /* ← polecenie */
}

header p {
  font-style: italic;
  font-size: 1.2em;
}

/* =========================
   NAWIGACJA
   ========================= */
nav,
footer {
  font-family: verdana, sans-serif;
  background-color: #783F27;
}

nav ul li a:link,
nav ul li a:visited {
  color: #F9AB33;
}

nav ul li a:focus,
nav ul li a:hover,
nav ul li a:active {
  color: #fff;
}

nav ul li {
  font-size: .8em;
  text-transform: uppercase;
  letter-spacing: .2em;
}

/* =========================
   SEKCJA GŁÓWNA
   ========================= */
main {
  font-family: 'Stint Ultra Expanded', cursive;
  background-color: white;
  line-height: 1.8em;
  color: #555;
}

/* ← polecenie: padding dla wszystkich elementów w main */
main * {
  padding: 1em;
}

h3 {
  text-transform: uppercase;
  letter-spacing: .2em;
  color: #7A0002;
}

p.more {
  font-family: verdana, sans-serif;
  text-transform: uppercase;
  font-size: .8em;
}

/* =========================
   ASIDE
   ========================= */
aside {
  background: url(images/scallop.png) repeat-y left top;
  background-color: #F6F3ED;
  padding: 1em;              /* ← polecenie */
  padding-left: calc(1em + 45px); /* ← polecenie */
}

/* =========================
   STOPKA
   ========================= */
footer {
  color: #EADDC4;
  text-align: center;
  font-size: .8em;
  padding: 1em;              /* ← polecenie */
}

/* =========================
   POZOSTAŁE
   ========================= */
h2 {
  font-family: 'Stint Ultra Expanded', cursive;
}

.day {
  color: #783F27;
  font-family: verdana, sans-serif;
  font-size: .8em;
  text-transform: uppercase;
}









